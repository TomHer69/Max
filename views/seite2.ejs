<!DOCTYPE html>
<html lang="de">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Seite 2</title>
    <style>
        :root {
            --bg: #0b0f14;
            --card: #10161d;
            --muted: #748090;
            --text: #e6eef8;
            --accent: #5aa9ff;
            --border: #22303d;
            --success: #26c281;
            --warn: #ffb400;
        }

        * {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            padding: 24px;
            font-family: system-ui, -apple-system, Segoe UI, Roboto, Inter, Arial, sans-serif;
            background: linear-gradient(180deg, #0b0f14, #0e141b 50%, #0b0f14);
            color: var(--text);
        }

        h1 {
            margin: 0 0 16px;
            font-size: 24px;
            letter-spacing: .2px;
        }

        a {
            color: var(--accent);
            text-decoration: none;
        }

        a:hover {
            text-decoration: underline;
        }

        .panel {
            background: var(--card);
            border: 1px solid var(--border);
            border-radius: 16px;
            padding: 16px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, .35);
            max-width: 500px;
            margin: 0 auto;
        }

        img {
            display: block;
            margin: 24px auto;
            max-width: 100%;
            border-radius: 12px;
            box-shadow: 0 4px 16px rgba(0, 0, 0, 0.2);
        }
    </style>
</head>



<body>
    <div class="panel">
        <h1>
            <%= ueberschrift %>
        </h1>
        <img src="/images/bild2.jpg" alt="Bild 2">
        <p><a href="/">Zur Seite 1</a></p>
    </div>
</body>

</html>